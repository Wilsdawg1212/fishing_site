import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#EAEAEA',
    },
    secondary: {
      main: '#055C32',
    },
  },
});

export default theme;
